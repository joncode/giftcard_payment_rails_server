<!DOCTYPE html>
<html>
<head>
  <title><%= PAGE_NAME %></title>

  <!-- iOS homescreen icons -->
  <link rel="apple-touch-icon-precomposed" href="touch-icon-iphone.png" />
  <link rel="apple-touch-icon-precomposed" sizes="72x72" href="touch-icon-ipad.png" />
  <link rel="apple-touch-icon-precomposed" sizes="114x114" href="touch-icon-iphone@2x.png" />
  <link rel="apple-touch-icon-precomposed" sizes="144x144" href="touch-icon-ipad@2x.png" />

  <%= render 'layouts/shim' %>
  <%= stylesheet_link_tag    "application" %>
  <%#= stylesheet_link_tag    "mobile" if mobile_device? %>
  <%= javascript_include_tag "application" %>

  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <% Time::DATE_FORMATS[:custom] = "%B %e at %I:%M %p" %>
  <% Time::DATE_FORMATS[:custom_form] = "%b %e" %>

  <link href='http://fonts.googleapis.com/css?family=Oswald:400,300,700' rel='stylesheet' type='text/css'>
</head>

<body>

  <div class="centerMainWrapper wholePageOBJ">
      <% if signed_in? %>
          <%= render 'layouts/header' %>

          <% if params[:controller] == "merchants" && @provider %>
            <%= render 'merchant_options', tab: params[:action] %>
          <% elsif params[:controller] ==  "brands" && @brand && !@brand.new_record? %>
            <%= render 'brands/menu', tab: params[:action] %>
          <% elsif params[:controller] == "providers" %>
            <% if @provider && !@provider.new_record? %>
              <%= render 'providers/menu', tab: params[:action] %>
            <% end %>
          <% elsif params[:controller] == "menus" %>
            <% if @provider && !@provider.new_record? %>
              <%= render 'providers/menu', tab: params[:action] %>
            <% end %>
          <% end %>
      <% else %>
          <%= render 'layouts/intro' %>
      <% end %>
      <p id="notice"><%= notice %></p>
      <%= yield %>

  </div>

</body>

</html>
