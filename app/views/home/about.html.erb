<div class="center borderBox">
  <div class="row">
    <h1><strong><%= PAGE_NAME %></strong>&nbsp;<%= image_tag "88-beer-glass-gray.png" %></h1>
    <h2>
      "Everything starts with a drink"

    </h2>
    <% if !signed_in? %>
      <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
    <%  end %>
  </div>
</div>